# =============================================================================
# configs/base/mediatek-mt81xx.config
#
# Placeholder - intentionally empty.
# The build pipeline falls back to hexdump0815's config.cbm as the ARM64 base:
#   ARM64_EXTERNAL_REPO_URL_MEDIATEK in kernel_versions.conf
#
# Populate this file to override hexdump's base for all mediatek-mt81xx builds.
# =============================================================================
